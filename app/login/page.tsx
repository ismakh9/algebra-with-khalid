import { SchoolLogin } from '@/components/school-auth';
export const dynamic = 'force-static';
export const metadata = { title: 'School login — Algebra with Khalid' };
export default function LoginPage() { return <SchoolLogin />; }
